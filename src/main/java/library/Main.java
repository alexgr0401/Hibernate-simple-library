package library;


// Commented all because of the use of hibernate


//import java.util.ArrayList;
//import java.util.List;
//import java.util.Set;
//import com.sun.source.tree.WhileLoopTree;
//import java.sql.Date;
//import java.util.Iterator;
//import java.util.List;
//import sql.SqlConnection;
//import java.sql.ResultSet;
//import levelup.Librarian;
//import sql.SqlUtilities;

public class Main {

	public static void main(String[] args) {

//		String titlePart = "pr";
//		ResultSet rs = SqlUtilities.getBookByTitlePart(titlePart);

//		Configuration conf = new Configuration().configure();
//		conf.addAnnotatedClass(Librarian.class);
//		ServiceRegistry sreg = new ServiceRegistryBuilder().applySettings(conf.getProperties()).buildServiceRegistry();
//		SessionFactory sf = conf.buildSessionFactory(sreg);
//		Session session = sf.openSession();

//		Librarian librarian = new Librarian(6, "Petar", "Petrović", 1, "user", "pass");
//		Transaction tx = session.beginTransaction();
//		session.save(librarian);
//		tx.commit();

	}
}
